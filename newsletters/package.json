{
	"name": "newsletters",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"deploy:prod": "wrangler deploy -e prod",
		"dev": "wrangler dev",
		"test": "NODE_OPTIONS=--experimental-vm-modules npx vitest run --coverage"
	}
}

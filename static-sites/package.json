{
	"name": "api-keys",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"deploy:prod": "wrangler deploy -e prod",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "NODE_OPTIONS=--experimental-vm-modules npx vitest run --coverage",
		"init": "node ./scripts/r2-init.mjs"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240129.0",
		"@vitest/coverage-v8": "^1.2.2",
		"vitest-environment-miniflare": "^2.14.2",
		"tslib": "^2.6.2",
		"typescript": "^5.3.3",
		"vitest": "^1.2.2",
		"wrangler": "^3.27.0"
	}
}
